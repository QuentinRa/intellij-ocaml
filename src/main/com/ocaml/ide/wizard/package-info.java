/**
 * This package is used to provide an OCaml module Builder.
 * This is the page that will be shown
 * - when creating a new project (wizard or File > New > Project)
 * - when creating a new module (Project Structure > Modules > +, or File > New > Module)
 * <p>
 * We got two steps (aside the one with the project folder, etc.)
 * - pick an OCaml SDK
 * - select (or not) a template
 */
package com.ocaml.ide.wizard;